#include <iostream>
using namespace std;
int main()
{
    int n;
    cout<<"Enter the size of array: ";
    cin>>n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }
   for (int count = 0; count< n; count++)
   {
      for (int i = 0; i < count-1; i++)
      {
         
      }
       
   }
   
    
    return 0;
}